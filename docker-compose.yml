services:
  mcp-ollama-account-rotation:
    container_name: mcp-ollama-account-rotation
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    command: ["--transport", "sse", "--port", "8000"]
